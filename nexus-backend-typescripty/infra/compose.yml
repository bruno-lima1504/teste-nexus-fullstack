name: 'nexus-backend-typescripty'

services:
  db:
    image: postgres:18-alpine
    container_name: nexus-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
